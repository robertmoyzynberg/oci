# Local development (direct to backend — requires CORS, which the API allows).
# For Vite proxy (recommended locally), use: VITE_API_URL=/api
VITE_API_URL=http://localhost:8000

# Production (Vercel): set this in the Vercel project env vars, e.g.
# VITE_API_URL=https://oci-backend.fly.dev
